// Rspack implements the webpack plugin API (compiler.webpack, hook names, emitAsset),
// so the same plugin works unchanged; see https://rspack.rs/api/plugin-api/.
export { OrxParallelWasmPlugin, orxParallelWasm, orxParallelWasm as default } from "./webpack.js";
